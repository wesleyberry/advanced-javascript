"use strict";
exports.__esModule = true;
// Types
var lucky;
lucky = '23';
lucky = 23;
var font;
var person = {
    first: 'Wesley',
    last: 'Berry'
};
// --------------------------------------------------------
// Functions
function pow(x, y) {
    return Math.pow(x, y);
}
pow('23', 'foo');
