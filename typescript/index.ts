import * as _ from 'lodash';

let lucky;

lucky = '23';
lucky = 23;

type Style = 'bold' | 'italic' | 23;

let font: Style;

// --------------------------------------------------------

interface Person {
    first: string;
    last: string;
}

const person: Person = {
    first: 'Wesley',
    last: 'Berry'
}

// --------------------------------------------------------